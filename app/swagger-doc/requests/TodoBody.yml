components:
    requestBodies:
        TodoBody:
        description: A JSON object containing todo information
        required: true
        content:
            application/json:
                schema:
                    $ref: '#/components/schemas/Todo'
