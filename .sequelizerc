const path = require('path');
const { db } = require('./src/config');

module.exports = {
    url: db.url,
    'models-path': path.resolve('src', 'models'),
    'seeders-path': path.resolve('src', 'seeders', 'dev'),
    'migrations-path': path.resolve('src', 'migrations')
};
